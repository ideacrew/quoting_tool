import { CoverageTypePipe } from './coverage-type.pipe';

describe('CoverageTypePipe', () => {
  it('create an instance', () => {
    const pipe = new CoverageTypePipe();
    expect(pipe).toBeTruthy();
  });
});
